<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Formulaire de contact</title>
    </head>
    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .email-container {
            max-width: 800px;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .email-header {
            text-align: center;
            background-color: #444;
            color: #ececec;
            width: 100%;
            border: 1px solid #444;
            padding: 10px 20px;
            margin-top: 0;
            border-radius: 5px 5px 0 0;
            text-transform: uppercase;
        }

        .email-body {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            padding: 10px 20px;
            border: 1px solid #444;
            border-radius: 0 0 5px 5px;
            width: 100%;
        }

        .email-body .email-row {
            display: flex;
            flex-direction: row;
            gap: 10px;
            width: 100%;
        }

        .email-body .email-row p:first-child {
            width: 20%;
            text-align: end;
            font-weight: bold;
        }

        .email-body .email-row p:last-child{
            width: 80%;
            text-align: start;
        }
    </style>
    <body>
        <div class="email-container">
            <div class="email-header">
                <h1>Formulaire de contact</h1>
            </div>
            <div class="email-body">
                <p>Bonjour,<br><br>Vous avez reçu un nouveau message via le formulaire de contact :</p>
                <div class="email-row">
                    <p><u>Email</u> : </p>
                    <p>{{ email }}</p>
                </div>
                <div class="email-row">
                    <p><u>Sujet</u> : </p>
                    <p>{{ subject|unescape|nl2br }}</p>
                </div>
                <div class="email-row">
                    <p><u>Message</u> : </p>
                    <p>{{ message|unescape|nl2br }}</p>
                </div>
            </div>
        </div>
    </body>
</html>
